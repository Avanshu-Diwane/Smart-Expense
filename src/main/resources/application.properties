spring.application.name=smartexpensetracker

# Database - Added default values for local safety
spring.datasource.url=${DATASOURCE_URL:jdbc:mysql://localhost:3306/smartexpense}
spring.datasource.username=${DATASOURCE_USER:root}
spring.datasource.password=${DATASOURCE_PASSWORD:yashuu}

# FIX: Match the name used in SecurityConfig
FRONTEND_URL=${FRONTEND_URL:http://localhost:3000}
app.jwt.secret=${JWT_SECRET:mysupersecretkeymysupersecretkeymysupersecretkey}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Critical for Deployment
server.port=${PORT:8080}